package de.knowwe.jurisearch.usersupport.word;


/**
 * Handles html where the tree structure is encoded in a mixture of
 * headlines (&lt;h1&gt;,...,&lt;h9&gt;) and explicit dashes ("-", "--", ..).
 * Also, the html document may contain an index, that remains as artifact from a
 * conversion from word (it will be ignored).
 * @author boehler
 *
 */
public class JuriSearchChapterHtmlToWikiSyntaxConverter {

}
